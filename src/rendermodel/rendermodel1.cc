#include "rendermodel.ih"

RenderModel::RenderModel()
:
    d_drawcount(0),
    d_projection_location(-1),
    d_program(-1),
    d_vertex_buffer(-1),
    d_color_buffer(-1),
    d_attribute_object(-1)
{
    
}